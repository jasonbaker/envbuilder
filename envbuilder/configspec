[project]
parcels = force_list()
requires = force_list(default=list())
easy_install = string(default='$CWD/bin/easy_install')
    [[DEFAULT]]
    [[__many__]]
       # name is defined in config.py
       dir = string(default='$name')
       url = string()
       ___many___ = force_list(default=list())
# This is currently unused
[commands]
	[[__many__]]
	required = boolean(default=True)
	default = force_list(default=list())
	working_dir = string(default='%dir')
	help = string(default='')